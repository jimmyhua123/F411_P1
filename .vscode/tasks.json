{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Deep Clean Ninja",
            "type": "shell",
            "command": "del ${workspaceFolder}/build/Debug/.ninja_lock",
            "windows": {
                "command": "if (Test-Path ${workspaceFolder}/build/Debug/.ninja_lock) { Remove-Item ${workspaceFolder}/build/Debug/.ninja_lock }"
            },
            "problemMatcher": []
        }
    ]
}